extends layout

block content
  h1= title
  p Welcome to #{title}
  p
    a(href = '/login') 登录

  :markdown-it(linkify='linkify' langPrefix='highlight-' )
    # Markdown
    >scroll-view 需要设置固定高度可以通过，如果已知其他固定的元素的高度，那么可以通过设置height：calc（100vh - 固定的px），这样就可以对不同高度的手机做适配；
    
    Markdown document with http://links.com and

    ```js
    var codeBlocks;var codeBlocks;var codeBlocks;
    var code;
    ```
    
  p 
    :markdown-it(inline='true') **加粗文字**